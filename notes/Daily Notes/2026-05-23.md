[[../Claude Context]] | [[../Experiment Story]] | [[2026-05-22]]

# 2026-05-23

## Goal — Finish the Detection-Level Numbers

End-of-day [[2026-05-22]] left two big open items: (1) `tune_detection_threshold`
was prohibitively slow (45 redundant detections per sweep, ~6 h), and (2) no
detection-level precision/recall on the held-out test set yet — the headline
number for the presentation. Today: kill (1), get end-to-end to (2).

---

## `tune_detection_threshold` Rewritten — Cached-Score Sweep

Old version: for each of 15 thresholds, call `process_image` on each of 3 val
images → **45 full multi-scale detections** per sweep, with only the final
score-filter changing. ~6 h, the reason yesterday's run was stopped.

New version: run multi-scale detection **once per val image** with
`cfg.detection_threshold = -np.inf` to capture every candidate window pre-NMS.
Cache `(all_dets, all_scores, gt_boxes)` per image. The sweep then filters the
cached scores per threshold and runs NMS only on the survivors. **3 detections
total per sweep**, ~30 min instead of 6 h.

### Why filter-then-NMS gives identical above-threshold survivors as NMS-then-filter

NMS is processed in descending score order. For any above-threshold box `B_j`,
its kept/suppressed status depends only on which higher-scoring boxes have
already been kept — none of the lower-scoring (would-be-filtered-out) boxes
matter. So whether we filter before or after NMS, the above-threshold survivors
are the same set. This justifies dropping the per-threshold redetection
entirely, and is recorded in a comment on the sweep loop.

### Why the pyramid loop is inlined, not reusing `multi_scale_detect`

`multi_scale_detect` → `non_max_suppression` returns boxes only — the scores
are dropped at NMS. We need scores to cache, so we can't call it. The clean
alternative would be to refactor `multi_scale_detect` to return `(boxes,
scores)` plus an `apply_nms: bool=True` flag and update the other two callers
(HNM loop + test eval). Decided not to: the inlined block is ~10 lines, the
refactor ripples to two call sites for no near-term gain. Inlined version
stays.

---

## First End-to-End papermill Run (PID 107865) — ~70 Minutes

Notebook had been edited in VS Code overnight: HNM loop in cell 27 commented
out (deliberate — keep yesterday's HNM-trained `svm_clf` from
`microglia-artifacts/`), executor split into a small cell 28 doing only
`fit_pipeline` + `tune_detection_threshold` + a final-threshold print. Total
cell count moved from 31 → 32 (my earlier rewrite-script run had not split
cells; the split was the VS Code edit).

Run completed cells 1–29; **threshold tuning succeeded**:

| Best | t | Precision | Recall | F1 |
|---|---|---|---|---|
|   | **3.92** | 0.235 | 0.359 | **0.284** |

Per-threshold sweep tail (full table in the next section):

| t | P | R | F1 |
|---|---|---|---|
| 3.92 | 0.235 | 0.359 | 0.284 |
| 5.88 | 0.314 | 0.209 | 0.251 |
| 7.84 | 0.488 | 0.131 | 0.206 |
| 9.80 | 1.000 | 0.046 | 0.087 |
| 13.72 | 1.000 | 0.007 | 0.013 |

Precision climbs monotonically with t (classifier is well-calibrated in the
score sense), recall collapses early. The F1-optimum sits firmly on the
low-precision, modest-recall side.

### Crash at Cell 30 (FINAL EVAL)

```
ValueError: Found array with 0 feature(s) (shape=(2, 0))
while a minimum of 1 is required by StandardScaler.
```

`extract_features` returned 0 vectors for both `test microglia` and `test
noise`, so `X_test` came out as `(2, 0)` and `scaler.transform` rejected it.

### Root Cause — Latent Cold-Start Gap

`Processed_training_images/Test/{Microglia,Noise}` are empty on disk
(timestamp May 16 — never touched since folder creation). `preprocess_all_images()`
is the only thing that populates them, and its call at the bottom of cell 10
is commented out (cold-start path). `features_cache.npz` stores **train/val**
raw features only — never test patches. The cold-start cell faithfully loads
the cache, but there's no "ensure test patches" step, so the Test folders
stay empty.

**Why this only surfaced today**: no prior end-to-end run had reached cell 30.
[[2026-05-20]]'s run crashed pre-HNM; [[2026-05-22]]'s was stopped during the
slow threshold tuning. Today's fast tuner reached the test eval for the first
time and exposed the gap.

### Side Observation — Artifacts Unchanged

The HNM loop's `joblib.dump` calls are commented out along with the loop, and
cell 28 only re-fits in-memory. So `microglia-artifacts/{svm_clf,scaler,pca}.pkl`
are still yesterday's HNM-trained versions, untouched by this run. The
in-memory `scaler`/`pca` produced by cell 28's `fit_pipeline` are deterministic
given the seed + same `X_train_raw`, so they match the cached versions — the
cold-start-loaded `svm_clf` remains consistent with the freshly fit feature
transform.

---

## Fix Applied — `ensure-test-patches` Block at Top of Cell 30

Idempotent, scoped to the test split, doesn't touch train/val. Skipped on
subsequent runs because `list_images(cfg.microglia_test_folder)` is no longer
empty. Includes a `RuntimeError` if zero positives are extracted — catches a
silent CSV-mismatch failure early. ROI sanity check passed: `Image_ROIs.csv`
has rows for both `13_s46.png` and `14_s47.png`. `load_gt_boxes` looks up by
`stem + ".png"`, so it matches the `.jpg` files in `test_dir/` (the lookup key
doesn't depend on the file's actual extension on disk).

Backup unchanged (`.bak` from yesterday is still the pre-rewrite snapshot).

---

## Why F1 = 0.284 — Quantitative Decomposition

Back-solving the t=3.92 row gives the per-image picture:

| | Per validate image (≈) |
|---|---|
| Sliding-window positions | 45,000 |
| Ground-truth cells | 50 |
| TP | 18 |
| FP | 62 |
| Specificity error | 62 / 44,950 = **0.14%** |

The classifier is actually 99.86% specific — quite good. The 0.284 F1 is not a
classifier-quality failure; it's three structural effects compounding:

1. **Class imbalance at inference.** 0.14% FPR × ~45k negatives still produces
   ~60 FPs/image, against only ~18 TPs. This is the project's own
   "patch metrics ≠ detection quality" thesis quantified — same model gives
   patch F1 ≈ 0.5 on the (balanced) validate split, but detection F1 ≈ 0.28
   when sweeping the full image. ~44% drop, same classifier.
2. **Pyramid coverage gap.** Scales (0.75, 1.0, 1.5) → effective window sizes
   43 / 64 / 85 px. Cells <32 px (p10) and >86 px (p90) fall outside any
   scale's IoU≥0.5 reachability. ~20% of the cell population unreachable. The
   extension to (0.5, 0.75, 1.0, 1.5, 2.0) sketched in [[2026-05-19]] would
   directly recover some of the lost recall, no retraining.
3. **IoU ≥ 0.5 strictness on grid-aligned windows.** No box regression in this
   pipeline (Dalal & Triggs lineage — see [[../research]]). A 32 px cell
   against a 43 px window maxes out at IoU = (32/43)² ≈ 0.55 when perfectly
   centered, dropping below 0.5 within a few pixels of offset. The 8 px stride
   doesn't always land near a cell's centre. A fraction of correctly-classified
   cells fail the IoU match anyway.

### Why this is Beat 3 / Beat 4 of the presentation, not a result-quality problem

`0.5 → 0.28` is the headline punchline replacing the 2023 patch-F1 = 0.92
claim. The drop quantifies why "looks good on paper, fails in production" — a
narrative pharma audiences will recognise. Future fixes (pyramid extension,
higher-capacity model, box regression, more labelled data) are concrete Q&A
follow-ups, not deficiencies to apologise for.

### What the test eval will likely show

Validate F1 is **slightly optimistic** because HNM mined hard-negative crops
from validate-image *backgrounds* into training (no GT contamination, but
specific FP-style examples did leak in). Test images were untouched. Expect
test F1 around or slightly below 0.28.

---

## VS Code "Notebook Controller Is Disposed"

Triggered when re-opening the notebook to run cell 30 interactively. Caused by
an orphan `ipykernel_launcher` (PID 96525) running in `/home/ec2-user/lx-svm-venv`
— leftover from a previous VS Code session whose connection had died. VS Code
was holding a reference to a kernel that was no longer reachable from its end.
Killed PID 96525. `jupyter kernelspec list` shows two specs (`python3` in the
venv + a user-local `lx-svm-venv`); both point to the same env, either is
selectable. ipykernel 7.2.0, RAM 12 GiB available — environment itself is
healthy, the issue was purely the dangling reference.

---

## Baseline & Augmentation — Framing for Beat 3 Ablation

For Beat 3 ("here's what was wrong + fixed"), the right *internal* baseline is
a **barebones SVM ablated within the current codebase** — same train/val/test
split, same evaluation harness, just turn off everything that was added.
Isolates the engineering contribution from confounders like split choice or
eval bugs.

### Recommended baseline configuration

| Component | Baseline | Current |
|---|---|---|
| Features | HOG-coarse only (Dalal & Triggs) | HOG-fine + HOG-coarse + colour hist + spatial bins + LBP + intensity stats |
| Dim reduction | none | PCA → 4,677 components |
| Hard negatives | random background only | HNM (4 rounds, +13,651 mined) |
| Pyramid | single scale (1.0) | (0.75, 1.0, 1.5) with cross-scale NMS |
| Threshold | default / patch-F1-tuned | detection-F1-tuned at IoU≥0.5 |
| Data leakage | **fixed in both** | fixed |
| Rotation augmentation | **on in both** | on |

Critical: the **leakage fix is applied in both** versions. If the baseline keeps
the leak, the comparison is unfair — the audience can rightly say "you only
beat your old self because you fixed a known bug." The bug fix is *one of the
ablated rows*, not a free pass.

### Why not other baselines

- `SVM_original.ipynb` as-is — has the leakage. Its patch F1 ≈ 0.92 is the
  *misleading headline*, useful for Beat 1's "looked good on paper" claim but
  not as a technical comparison point.
- Random Forest on the same features — changes the classifier, not the
  engineering. Doesn't test the right hypothesis.
- "Just cite the old number" — doesn't show what the new engineering bought,
  because the gap from 0.92 → 0.28 also includes the leakage-fix effect.

### Where rotation augmentation sits in the story — necessity, not improvement

Rotation augmentation is **not** a 2026 addition. It was in the 2023 pipeline
too; what changed is *where it sat in the pipeline order* (i.e. the
leakage fix — augment after split, not before). The augmentation itself is
an architectural necessity, not a claimed engineering win.

**Why it's a necessity for this architecture specifically**: HOG features bin
gradients into orientation channels — rotate the input and you get a *different*
feature vector. LinearSVC has zero rotation invariance baked in. With only
~800 training cells and no architectural invariance, the classifier would
overfit to the orientations that happened to appear in those 11 training
images and miss everything else at inference. Augmentation is how rotation
invariance is delivered **through the data** when it can't come from the model.

**Implication for the ablation table**: rotation augmentation is a *constant
background* in both rows — applied (post-split) in baseline and current alike.
The ablated row adjacent to it is the **data-leakage fix** ("before: augment
then split / after: split then augment"). Conflating "the pipeline needs this
to work" with "the engineering improved over baseline" would be a fair target
for interviewer pushback.

### Backup-slide ablation worth having ready

An optional "with vs without rotation augmentation" comparison (re-train
current pipeline with `augment=False` on the train microglia path → re-evaluate
detection) is strong Q&A material. The recall drop will likely be substantial,
and the framing — *"for a linear classifier on HOG features, augmentation is
the rotation-invariance mechanism the architecture can't provide"* — is the
kind of architectural-trade-off answer pharma audiences will respond to. Not
worth slide-time in the 7 min though; keep it as a backup.

### Two presentation formats

**Single-row** (cleanest for 7 min):

| | Detection F1 (IoU ≥ 0.5) |
|---|---|
| Baseline SVM (HOG only, single scale, no HNM) | _[fill]_ |
| Current pipeline (all additions) | 0.28 |

**Ablation row-by-row** (deeper, fits as a backup slide):

| | Detection F1 |
|---|---|
| Baseline (HOG only, single scale) | _[fill]_ |
| + Extra features (colour / LBP / intensity) | _[fill]_ |
| + PCA | _[fill]_ |
| + Multi-scale pyramid | _[fill]_ |
| + HNM | _[fill]_ |
| + Detection-level threshold tuning | **0.28** |

Single-row is the right level for the 7-minute story. Ablation table belongs
in Q&A — pharma audiences ask "which contribution mattered most?" frequently
and an ablation is exactly the right answer. ~2–3 h for the single-row,
~4–5 h for the full ablation; the fast threshold tuner from this morning
makes the ablation tractable (was hours per row before).

Headline number contrast for Beat 1↔Beat 4 stays at **leaky-2023 patch F1
≈ 0.92 vs. honest-2026 detection F1 ≈ 0.28**. The baseline-detection-F1 to
produce here is the *engineering-level* contrast — what the additions bought,
separately from the "patch ≠ detection" insight.

---

## Open Items — for 2026-05-24

1. **Verify the test-cell fix end-to-end.** Run cell 30 (interactively or via
   papermill) and capture:
   - Patch-level test metrics
   - Detection-level test metrics at IoU≥0.5 — **the headline presentation
     number**
   - Boxes-on-image plots for `13_s46` and `14_s47`
2. **Decide on the operating threshold for the test eval.** With cell 28's
   `tune_detection_threshold` line commented, `cfg.detection_threshold` will
   stay at the Config default `1.5` unless set explicitly. Either re-enable
   the tuning line, or hardcode `cfg.detection_threshold = 3.92` (today's
   tuned value) before cell 30 runs. The F1 surface around t=3.92 is sharp
   on both sides; using `1.5` would inflate FPs and crush precision.
3. **Pyramid extension to (0.5, 0.75, 1.0, 1.5, 2.0)** — sketched
   in [[2026-05-19]], not yet applied. Lowest-effort recall win available,
   no retraining.
4. **YOLOv8-nano fine-tune** for Beat 4 — still untouched. Allocated 3 h in
   the original plan ([[2026-05-06]]).
5. **`Experiment Story.md` gaps** — every `[fill in]` field for the 2023
   experiments (patch F1, FP counts, ensemble vs single-window comparison)
   still empty. The 2026 sections now have concrete numbers to plug in;
   2023 sections still need archaeology.

## Artefacts on Disk (end of session)

- `2026/SVM_original_refactored.ipynb` — cell 27 (tune_detection_threshold
  cached-score rewrite), cell 30 (ensure-test-patches block). Both ast-parse
  clean. Cell count 32.
- `2026/SVM_original_refactored.ipynb.bak` — pre-rewrite snapshot (from
  yesterday's working state, preserved through today's edits).
- `2026/SVM_original_refactored_output.ipynb` — papermill output of today's
  PID 107865 run, captures the threshold-sweep plot + per-threshold P/R/F1
  table. Useful primary source for Beat 3/Beat 4 plot generation.
- `microglia-artifacts/` — **unchanged** from yesterday (HNM-trained
  `svm_clf.pkl`, `scaler.pkl`, `pca.pkl`, `hnm_train_final.npz`,
  `features_cache.npz`). Today's run only re-fit in-memory.

---

## Afternoon — Ablation Pipeline + First Four Rows

Pivot from the morning's "make the refactored notebook run end-to-end" to
"produce an ablation row-by-row table for the presentation". Result: a clean
multi-notebook pipeline + 4 of 5 rows landed with concrete detection F1 on the
held-out test set.

### Refactor — `pipeline.py` + 5 thin notebooks

Replaced the single-notebook pattern with a shared module:

```
2026/
├── pipeline.py                        ← shared functions (~600 lines)
├── compute_variance_curve.py          ← one-off PCA Gram-trick
├── ablation_1_baseline.ipynb          ← TRAIN
├── ablation_2_features.ipynb          ← TRAIN
├── ablation_3_multiscale.ipynb        ← INFERENCE (loads row 2 pkls)
├── ablation_4_hnm.ipynb               ← INFERENCE (loads HNM model from microglia-artifacts-hnm)
└── ablation_5_threshold.ipynb         ← INFERENCE (will load row 4 pkls)
```

`pipeline.py` exposes everything as functions and the row-by-row deltas
become two `Config` switches:

| Switch | Values | Used by |
|---|---|---|
| `feature_mode` | `'hog_coarse'` / `'full'` | extract_features, sliding_window_detect (branches inside) |
| `scale_factors` | `(1.0,)` / `(0.75, 1.0, 1.5)` | multi_scale_detect (single-elem tuple ≡ single-scale) |

Each ablation notebook = 6-10 cells of orchestration; no duplicated logic.
Removed legacy `SVM_original_fixed.ipynb` (the 2023 file) since
`ablation_1_baseline.ipynb` supersedes it. `SVM_original_refactored.ipynb`
left untouched as the WIP reference.

### PCA Variance Curve — Row 1 needs its own int

Refactored matches the same pattern as the morning notebook: `Config.pca_n_components`
is a hardcoded int, the int chosen offline once via Gram-trick variance curve.
For row 1 (HOG-coarse, ~5292-d) the rows 2-5 int (4677) doesn't apply — needed
a fresh Gram-trick run on the HOG-coarse feature space.

`compute_variance_curve.py` reads cached features, computes the smaller of
`X·X^T` (n_samples²) and `X.T·X` (n_features²), runs `eigvalsh`, prints
crossover table. For HOG-coarse, n_features=5292 < n_samples=14914 → use
`X.T·X` (5292×5292 float32 = ~110 MB; total peak ~500 MB).

| Variance target | n_components |
|---|---|
| 50% | 200 |
| 80% | 810 |
| 85% | 989 |
| **90%** | **1215**  ← chosen |
| 95% | 1540 |
| 99% | 2745 |

HOG-coarse needs ~23% of dims for 90% (vs full pipeline's 14%) — fewer
correlated channels means less compressible. Hardcoded `pca_n_components=1215`
into `ablation_1_baseline.ipynb`. Plot at
`microglia-artifacts-row1/pca_variance_curve_hog_coarse.png`.

### Data-Provenance Discipline Saved Slip

Tempted to slice the last 5292 dims off the May 22 features cache to get
row 1's HOG-coarse features (math is identical — `extract_features` puts
HOG-coarse at the end of the 32801-d vector). User flagged this as a
shortcut that bypasses the row 1 pipeline. Right answer: run
`ablation_1_baseline.ipynb` fresh to populate `microglia-artifacts-row1/`
from disk patches — the cache must be reproducible from the row's own
pipeline. Saved as a memory rule (`feedback-ablation-data-provenance`).

### OOM #3 — Float64 Feature Vectors in `extract_features`

Row 2's papermill run (PID 169675) died at the np.vstack step right after
feature extraction completed. dmesg: `Out of memory: Killed process 169675
(python3) total-vm:14471060kB, anon-rss:13599844kB`. Smoking gun.

**Root cause:** `extract_features` returns each vector as float64 (skimage's
`hog()` returns float64, `np.concatenate` upcasts everything). 17,950 vectors
of 32,801 dims in float64 = 4.7 GB in the Python list. Then `np.vstack`
allocates a 3.9 GB float64 result while the source list is still alive, then
`.astype(np.float32)` allocates *another* 1.96 GB. Transient peak ~10.5 GB.

Plus contributing factors:
- Two orphan ipykernels from earlier interactive sessions: PID 139209 (idle,
  52 MB) and PID 159692 (1.5 GB).
- VS Code Pylance server: ~1.3 GB.

Total at the cliff: ~14 GB, dmesg killed at 13.6 GB anon-rss → matches.

**Why row 1 didn't trip this:** HOG-coarse is 5292-d not 32801-d, so the
float64 list is only 0.63 GB — under any pressure.

**Important nuance:** the morning refactored notebook has the **same** float64
code. It succeeded on 2026-05-22 because nothing else was eating headroom.
The fix is the same code path for the May 22 run and today's row 2 — only
the in-memory transient changes; the **cached features on disk are float32
in both cases** (post-vstack astype).

### Fix — Float32 at Source + Eager Cleanup

`pipeline.py:extract_features`:
```python
features.append(compute(raw, cfg).astype(np.float32, copy=False))
```

`pipeline.py:extract_raw_features` (after each vstack):
```python
del train_pos_feats, train_neg_feats
gc.collect()
```

Peak drops from ~10.5 GB → ~3 GB. Killed the two orphan kernels too (~1.5 GB
freed). After both fixes the row 2 relaunch completed cleanly.

### Held-Out Test Detection F1 — Rows 1 to 4

All at IoU ≥ 0.5 on the 2 test images (13_s46.jpg, 14_s47.jpg, 245 GT cells
total — `evaluate_detections` greedy-matched).

| Row | Setup | TP | FP | FN | P | R | **F1** |
|---|---|---|---|---|---|---|---|
| 1 | HOG-coarse, PCA(1215), single scale, t=1.5 | 81  | 758  | 164 | 0.097 | 0.331 | **0.149** |
| 2 | + extra features, PCA(4677), single scale  | 91  | 1008 | 154 | 0.083 | 0.371 | **0.135** |
| 3 | + multi-scale (0.75, 1.0, 1.5) — load row 2 model | 156 | 3790 | 89 | 0.040 | 0.637 | **0.074** |
| 4 | + HNM (4 rounds, loaded `microglia-artifacts-hnm`) | 71  | 330  | 174 | 0.177 | 0.290 | **0.220** |
| 5 | + detection-threshold tuning (pending) | — | — | — | — | — | — |

Story across the rows:
- **Row 1 → 2** (+extra features): higher recall (+0.04) but more FPs at the
  default threshold → F1 drifts down. Extra features alone don't help at an
  untuned operating point.
- **Row 2 → 3** (+pyramid): recall jumps (+0.27) — pyramid catches small cells
  and large cells the 64-px window missed — but FPs nearly 4× because each
  scale adds independently. The biggest F1 drop in the series.
- **Row 3 → 4** (+HNM): FPs collapse 10× (3790 → 330) while recall halves. The
  HNM-trained boundary is more conservative; the default t=1.5 is now too
  aggressive (model expects to be operated at higher t). Row 5 should recover.
- **Pyramid + threshold-tuning interaction**: the 0.040 precision in row 3 is
  the strongest single argument for tuning the threshold *against detection F1
  on full images*, not patch F1. Headline of beat 3 of the presentation.

### Row 4 — Loaded From Cached HNM Artifacts

Realised the `microglia-artifacts-hnm/` artifacts from the morning's full-pipeline
run **are exactly the row 4 model** — same code (refactored into `pipeline.py`),
same fit-once design, same colour-fix, same 4 HNM rounds with fixed C. Threshold
tuning produces a number, not a new model, so the post-HNM pkls are valid for row 4.

Copied `microglia-artifacts-hnm/` → `2026/microglia-artifacts-row4/` and rewrote
`ablation_4_hnm.ipynb` to skip Sections 2-3 (initial fit + HNM loop) and only
load the pkls + run test eval. Saved ~70 min. Verified the loaded model:
`C=10.0, n_components_=4677, variance_retained=89.8%` — matches the artifact
fingerprint.

Note: this is **not** the same as the data-provenance shortcut from earlier
(slicing features). The HNM artifacts are literally the row 4 outputs of the
same algorithm; the only thing that changed is the codebase organisation.

### Open Items — for 2026-05-24

1. **Run row 5** — load `microglia-artifacts-row4/` pkls, run
   `tune_detection_threshold` on validate (cached-score sweep, ~30 min), then
   re-eval test at the tuned threshold. The row 5 F1 is the **headline
   pipeline number** for beat 4 of the presentation.
2. **YOLOv8-nano fine-tune** — still untouched. Allocated 3 h in original plan.
3. **`Experiment Story.md` 2023 gaps** — patch F1, FP counts, ensemble vs
   single-window comparison — still need archaeology.
4. **Pyramid extension to (0.5, 0.75, 1.0, 1.5, 2.0)** — sketched 2026-05-19,
   not yet applied. Cheapest recall win available.

### Artefacts on Disk (end of afternoon session)

| Path | Contents |
|---|---|
| `2026/pipeline.py` | Shared module — Config, I/O, preprocessing, features, training, detection, HNM, threshold tuning. Float32 fix applied. |
| `2026/compute_variance_curve.py` | Gram-trick PCA variance curve. |
| `2026/ablation_{1..5}_*.ipynb` | 5 ablation notebooks. |
| `2026/ablation_{1..4}_*_output.ipynb` | Papermill outputs with embedded detection figures. ~2.5 MB each. |
| `2026/run_row{1..4}.log` | Papermill stdout per row. |
| `2026/microglia-artifacts-row1/` | HOG-coarse model (svm_clf.pkl, scaler.pkl, pca.pkl, features_cache.npz, pca_variance_curve_hog_coarse.png). |
| `2026/microglia-artifacts-row2/` | Full-features model (~1.8 GB total — features_cache.npz is the big one). |
| `2026/microglia-artifacts-row4/` | Copy of `../microglia-artifacts-hnm/` — HNM-trained model. ~2.3 GB. |
| `../microglia-artifacts-hnm/` | Original (untouched) HNM artifacts from the morning's run. |
